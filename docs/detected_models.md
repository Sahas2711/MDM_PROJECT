# detected_models.md
> Generated by: Full Repository Discovery Scan
> Confidence: High (evidence from file existence, import chains, inference code, and API bindings)

---

## Model 1: Random Forest Price Classifier (9-feature, CURRENT)

| Field | Detail |
|-------|--------|
| Model Name | Random Forest Price Classifier |
| Category | Supervised Classification (multi-class: Low / Medium / High) |
| File Location | `backend/models/crop_price_classifier.pkl` |
| Notebook Location | `Notebooks/Updated_MDM_crop_prices.ipynb` |
| Backend Usage | `backend/predict.py` — loaded in `_registry["random_forest"]`; used by `/predict` and `/smart-decision` |
| Frontend Exposure | SellTiming.jsx (via /predict), SmartDecision.jsx (via /smart-decision), ModelPerformance.jsx (via /predict with model_type=random_forest) |
| API Integration | `POST /predict?model_type=random_forest` |
| Status | **Fully Integrated** |
| Input | 9 features: min_price, max_price, state_encoded, district_encoded, market_encoded, commodity_encoded, variety_encoded, grade_encoded, arrival_date_encoded |
| Output | Class 0=Low, 1=Medium, 2=High + predict_proba |
| Confidence | High — file present, loaded at startup, inference code confirmed, API endpoint confirmed, frontend calls confirmed |

**Evidence**:
- `predict.py` line: `MODEL_FILES = {"random_forest": "crop_price_classifier.pkl", ...}`
- `predict.py` fallback: loads `crop_model.pkl` only if `crop_price_classifier.pkl` missing
- `model-integration.md`: "crop_price_classifier.pkl - Random Forest model (99.71% accuracy)"

---

## Model 2: ANN Price Classifier (9-feature)

| Field | Detail |
|-------|--------|
| Model Name | Artificial Neural Network Price Classifier |
| Category | Supervised Classification (multi-class: Low / Medium / High) |
| File Location | `backend/models/ann_model.h5` |
| Notebook Location | `Notebooks/Updated_MDM_crop_prices.ipynb` |
| Backend Usage | `backend/predict.py` — loaded in `_registry["ann"]` via `_load_keras_model()` |
| Frontend Exposure | ModelPerformance.jsx (via /predict with model_type=ann) |
| API Integration | `POST /predict?model_type=ann` |
| Status | **Fully Integrated** |
| Input | Same 9-feature scaled vector as Random Forest |
| Output | Softmax probabilities over 3 classes |
| Confidence | High — file present, Keras load code confirmed, API endpoint confirmed |

**Evidence**:
- `predict.py`: `MODEL_FILES = {..., "ann": "ann_model.h5", ...}`
- `model-integration.md`: "ann_model.h5 - Artificial Neural Network (99.53% accuracy)"
- `ModelPerformance.jsx`: `MODELS = [{key: 'ann', ...}]` — calls `/predict?model_type=ann`

**Note**: ANN uses softmax output; `predict_proba` not available via sklearn interface. `intelligence.py` falls back to spread-tier heuristic for confidence when `predict_proba` is absent. However, Keras model's `predict()` returns probabilities directly — `_predict_probabilities()` handles this via `entry.kind == "keras"` branch.

---

## Model 3: DNN Price Classifier (9-feature)

| Field | Detail |
|-------|--------|
| Model Name | Deep Neural Network Price Classifier |
| Category | Supervised Classification (multi-class: Low / Medium / High) |
| File Location | `backend/models/dnn_model.h5` |
| Notebook Location | `Notebooks/Updated_MDM_crop_prices.ipynb` |
| Backend Usage | `backend/predict.py` — loaded in `_registry["dnn"]` |
| Frontend Exposure | Not directly exposed in ModelPerformance.jsx live comparison (only RF, GB, ANN listed in MODELS array) |
| API Integration | `POST /predict?model_type=dnn` (accessible but not surfaced in UI) |
| Status | **Backend Only** (loaded, API accessible, but not in frontend model comparison UI) |
| Input | Same 9-feature scaled vector |
| Output | Softmax probabilities over 3 classes |
| Confidence | High — file present, load code confirmed |

**Evidence**:
- `predict.py`: `MODEL_FILES = {..., "dnn": "dnn_model.h5"}`
- `model-integration.md`: "dnn_model.h5 - Deep Neural Network (63.75% accuracy)" — notably lower accuracy than ANN
- `ModelPerformance.jsx` MODELS array: only `random_forest`, `gradient_boosting`, `ann` — DNN missing

**Issue**: DNN is loaded and API-accessible but the frontend model comparison panel does not include it. The static benchmark chart in ModelPerformance.jsx shows DNN at 99.1% accuracy — this contradicts the notebook export note of 63.75%. This is a **fake metric** in the UI.

---

## Model 4: CNN Food Quality Classifier (Freshness)

| Field | Detail |
|-------|--------|
| Model Name | CNN Food Quality / Freshness Classifier |
| Category | Binary Image Classification (Fresh / Rotten) |
| File Location | `backend/models/cnn_food_quality_model.h5` |
| Notebook Location | `Notebooks/Updated_MDM_crop_prices.ipynb` |
| Backend Usage | `backend/predict_image.py` — loaded as `_freshness_model`; input 128x128 RGB |
| Frontend Exposure | Dashboard.jsx (/predict-image), ModelPerformance.jsx (/predict-image), SmartDecision.jsx (via /smart-decision) |
| API Integration | `POST /predict-image`, `POST /smart-decision` |
| Status | **Fully Integrated** |
| Input | 128x128 RGB image, normalized to [0,1] |
| Output | Softmax [Fresh_prob, Rotten_prob] |
| Confidence | High — file present, load code confirmed, inference code confirmed, multiple frontend bindings confirmed |

**Evidence**:
- `predict_image.py`: `FRESHNESS_MODEL_PATH = MODELS_DIR / "cnn_food_quality_model.h5"`
- `predict_image.py`: `FRESHNESS_CLASS_NAMES = ["Fresh", "Rotten"]`
- `Dashboard.jsx`: calls `fetchPredictImage(imageFile)` → `POST /predict-image`

---

## Model 5: Fruit Type Classifier (20-class)

| Field | Detail |
|-------|--------|
| Model Name | Fruit Type Classifier |
| Category | Multi-class Image Classification (20 classes: fresh/rotten × 10 fruit types) |
| File Location | `backend/models/fruit_classifier.h5` |
| Notebook Location | Not found in notebooks — likely trained separately or downloaded |
| Backend Usage | `backend/predict_image.py` — loaded as `_fruit_model`; input 224x224 RGB crop from YOLO bbox |
| Frontend Exposure | Indirectly via SmartDecision.jsx (fruit_detected, fruit_label, fruit_confidence in response) |
| API Integration | Used internally by `/predict-image` and `/smart-decision` |
| Status | **Fully Integrated** |
| Input | 224x224 RGB crop extracted from YOLO bounding box |
| Output | 20-class softmax (fresh apple, rotten apple, fresh banana, ...) |
| Confidence | High — file present, load code confirmed, class names hardcoded in predict_image.py |

**Evidence**:
- `predict_image.py`: `FRUIT_MODEL_PATH = MODELS_DIR / "fruit_classifier.h5"`
- `predict_image.py`: `FRUIT_CLASS_NAMES = ["fresh apple", "fresh banana", ..., "rotten bell pepper"]` — 20 classes
- `predict_image.py`: `_fruit_model.predict(_preprocess_cv(crop, FRUIT_IMG_SIZE), verbose=0)`

**Note**: Training source unknown — not found in the two notebooks. Likely a pre-trained model downloaded from a public dataset (e.g., Kaggle Fresh and Rotten Fruits dataset).

---

## Model 6: YOLOv8 Nano Fruit Detector

| Field | Detail |
|-------|--------|
| Model Name | YOLOv8 Nano (yolov8n) |
| Category | Object Detection |
| File Location | `backend/models/yolov8n.pt` |
| Notebook Location | Not in notebooks — standard Ultralytics pretrained weights |
| Backend Usage | `backend/predict_image.py` — loaded as `_yolo_model`; detects apple/banana/orange/carrot |
| Frontend Exposure | Indirectly via SmartDecision node graph (fruit_detector_label, fruit_detector_confidence) |
| API Integration | Used internally by `/predict-image` and `/smart-decision` |
| Status | **Fully Integrated** |
| Allowed Labels | apple, banana, orange, carrot (YOLO_ALLOWED_LABELS) |
| Confidence Threshold | 0.35 (YOLO_FRUIT_THRESHOLD) |
| Confidence | High — file present, Ultralytics YOLO load confirmed, detection loop confirmed |

**Evidence**:
- `predict_image.py`: `YOLO_MODEL_PATH = MODELS_DIR / "yolov8n.pt"`
- `predict_image.py`: `_yolo_model = YOLO(str(YOLO_MODEL_PATH))`
- `predict_image.py`: `YOLO_ALLOWED_LABELS = {"apple", "banana", "orange", "carrot"}`

**Note**: yolov8n.pt is the standard pretrained COCO weights, not fine-tuned on agricultural data. Only 4 of 80 COCO classes are used.

---

## Model 7: KMeans Market Clusterer

| Field | Detail |
|-------|--------|
| Model Name | KMeans Market Clusterer |
| Category | Unsupervised Clustering |
| File Location | `backend/models/kmeans_clusterer.pkl` |
| Notebook Location | `Notebooks/Updated_MDM_crop_prices.ipynb` |
| Backend Usage | `backend/predict.py` — `cluster_features()` function; called after price prediction |
| Frontend Exposure | SmartDecision.jsx (cluster_id, cluster_distance, cluster_analysis node) |
| API Integration | Used internally by `/predict` and `/smart-decision` |
| Status | **Fully Integrated** |
| Input | Same 9-feature scaled float32 vector as price models |
| Output | cluster_id (int), cluster_distance (float) |
| Confidence | High — file present, load code confirmed, inference code confirmed, frontend display confirmed |

**Evidence**:
- `predict.py`: `KMEANS_PATH = MODELS_DIR / "kmeans_clusterer.pkl"`
- `predict.py`: `_kmeans = joblib.load(KMEANS_PATH) if KMEANS_PATH.exists() else None`
- `predict.py`: `cluster_features()` function with float32 cast fix documented

---

## Model 8: Random Forest Price Classifier (2-feature, LEGACY)

| Field | Detail |
|-------|--------|
| Model Name | Random Forest Price Classifier (Legacy) |
| Category | Binary Classification (HOLD / SELL) |
| File Location | `backend/models/crop_model.pkl` |
| Notebook Location | Likely `MDM_crop_prices_.ipynb` (original notebook) |
| Backend Usage | `backend/predict.py` — fallback only if `crop_price_classifier.pkl` missing |
| Frontend Exposure | None directly; used by streamlit-frontend |
| API Integration | Fallback only |
| Status | **Legacy Fallback** |
| Input | 2 features: min_price, max_price only |
| Output | 0=HOLD, 1=SELL |
| Confidence | High — file present, fallback load code confirmed |

**Evidence**:
- `predict.py`: `if "random_forest" not in _registry and (MODELS_DIR / "crop_model.pkl").exists(): ...`
- `streamlit-frontend/services/local_model.py`: `model = joblib.load(MODELS_DIR / "crop_model.pkl")`

---

## Model 9: VAE (Variational Autoencoder) — MISSING

| Field | Detail |
|-------|--------|
| Model Name | Variational Autoencoder |
| Category | Generative Model (synthetic data generation) |
| File Location | `backend/models/vae_weights.h5` — **FILE DOES NOT EXIST** |
| Notebook Location | `Notebooks/Updated_MDM_crop_prices.ipynb` (trained in notebook) |
| Backend Usage | None — not loaded anywhere in backend |
| Frontend Exposure | None |
| API Integration | None |
| Status | **Notebook Only / Dead Code** |
| Confidence | High — model-integration.md documents export code for vae_weights.h5; file absent from models/ directory |

**Evidence**:
- `model-integration.md`: "vae.save_weights(os.path.join(EXPORT_DIR, 'vae_weights.h5'))"
- No `vae_weights.h5` in `backend/models/` directory listing
- No import or load of VAE anywhere in backend Python files

---

## Model 10: Gradient Boosting — MISSING

| Field | Detail |
|-------|--------|
| Model Name | Gradient Boosting Price Classifier |
| Category | Supervised Classification |
| File Location | `backend/models/gb_model.pkl` — **FILE DOES NOT EXIST** |
| Notebook Location | Unknown — not confirmed in notebooks |
| Backend Usage | Referenced in BACKEND.md and CURRENT_WORKING_STATE.md as missing |
| Frontend Exposure | ModelPerformance.jsx references `gradient_boosting` in MODELS array — will return error |
| API Integration | `POST /predict?model_type=gradient_boosting` — returns 422 (model not loaded) |
| Status | **Dead Code / Missing** |
| Confidence | High — file absent, CURRENT_WORKING_STATE.md explicitly states "Missing" |

**Evidence**:
- `CURRENT_WORKING_STATE.md`: "Gradient Boosting model ❌ Missing — gb_model.pkl absent"
- `ModelPerformance.jsx`: `{key: 'gradient_boosting', label: 'Gradient Boosting', color: '#F7B955'}` — will fail at runtime
- Static benchmark chart shows "Grad. Boost 97.5%" — this is a **hardcoded fake metric**

---

## Summary Table

| Model | File | Status | Frontend Exposed | API Endpoint |
|-------|------|--------|-----------------|-------------|
| Random Forest (9-feat) | crop_price_classifier.pkl | ✅ Fully Integrated | ✅ Yes | /predict, /smart-decision |
| ANN (9-feat) | ann_model.h5 | ✅ Fully Integrated | ✅ Yes | /predict?model_type=ann |
| DNN (9-feat) | dnn_model.h5 | ⚠️ Backend Only | ❌ Not in UI comparison | /predict?model_type=dnn |
| CNN Freshness | cnn_food_quality_model.h5 | ✅ Fully Integrated | ✅ Yes | /predict-image, /smart-decision |
| Fruit Classifier | fruit_classifier.h5 | ✅ Fully Integrated | ✅ Indirect | /predict-image, /smart-decision |
| YOLOv8 Nano | yolov8n.pt | ✅ Fully Integrated | ✅ Indirect | /predict-image, /smart-decision |
| KMeans Clusterer | kmeans_clusterer.pkl | ✅ Fully Integrated | ✅ Yes | /predict, /smart-decision |
| RF Legacy (2-feat) | crop_model.pkl | ⚠️ Legacy Fallback | ❌ Streamlit only | Fallback only |
| VAE | vae_weights.h5 | ❌ Missing / Dead | ❌ No | None |
| Gradient Boosting | gb_model.pkl | ❌ Missing | ⚠️ UI references it (will error) | /predict?model_type=gradient_boosting → 422 |
